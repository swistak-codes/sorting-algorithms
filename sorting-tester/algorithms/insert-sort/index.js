const isortInPlace = require('./in-place');
const isortInPlaceRecursive = require('./in-place-recursive');
const isortBinary = require('./binary');
const isortList = require('./list');

module.exports = {
  isortInPlace,
  isortInPlaceRecursive,
  isortBinary,
  isortList,
};